{
  "_comment": [
    "Claude Code SessionStart hook:在 Skill 載入前先檢查更新,更新當下就生效。",
    "把 <SKILL_DIR> 換成本機 skill 實際路徑(例:C:/Users/you/.claude/skills/aigo-template-transfer",
    "或 ~/.claude/skills/aigo-template-transfer),再把 hooks 區塊合併進",
    "~/.claude/settings.json(全域)或 <專案>/.claude/settings.json(專案)。",
    "Windows 請用 python,macOS / Linux 用 python3。",
    "腳本只在有新版時才輸出,無更新/離線時完全靜默,不會污染 context。"
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "python \"<SKILL_DIR>/scripts/check_update.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
